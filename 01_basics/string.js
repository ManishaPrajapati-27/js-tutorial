const  name = "Manisha";

console.log(`I am ${name}.`)

const upGame = new String("Hollow Queen");

console.log(upGame.length);

// Make Array based on seperator
console.log(upGame.split(" "))

// For Know which element are there in that index number
console.log(upGame.charAt(4))

// For Know where is present Q in string
console.log(upGame.indexOf("Q"))

// Slice of string also provide negative index
console.log(upGame.slice(-9, 8))

// Note take negative index
console.log(upGame.substring(2, 8))

// Present or not
console.log(upGame.includes("Hollow"))

// Element in <b></b> tag
console.log(upGame.bold())

// Add (concatinate) string
console.log(upGame.concat(" with King"))







